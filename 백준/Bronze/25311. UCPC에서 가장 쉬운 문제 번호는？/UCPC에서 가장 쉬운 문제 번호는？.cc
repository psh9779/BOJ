#include <iostream>
using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	
	// 가장 쉬운 문제의 위치를 고정하므로 "A번이 고정되어있음"
	// 따라서, 연도별로 출제진이 의도한 가장 쉬운 문제의 번호는 'A'를 출력
	int y;
	cin >> y;
	cout << 'A';

	return 0;
}